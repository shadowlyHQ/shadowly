import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Shadowly - Monetize Your Creator Platform',
  description: 'Helping micro-creators and creators monetize their platforms and grow their business.',
  icons: {
    icon: '/shadowly.png',
    apple: '/shadowly.png',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
